

<%= link_to edit_task_path(task), remote: true do%>
	<button> Edit </button>
<% end %>

<%= link_to task, remote:true, method: :delete, data:{ comfirm: 'Are you sure ?'} do %>
	<button> Delete! </button>
<% end %>